import { TemplateRefDirective } from './template-ref.directive';

describe('TemplateRefDirective', () => {
  it('should create an instance', () => {
    const directive = new TemplateRefDirective();
    expect(directive).toBeTruthy();
  });
});
